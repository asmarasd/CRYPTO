# 📄 وثيقة: فلتر المرساة (Anchor-Distance Filter)

> **الغرض من هذه الوثيقة:** توثيق "فلتر المرساة" — ما هو، لماذا وُجد، عتباته، أين يُطبَّق، ونتائجه المقيسة — ليُحفظ يدويًّا بعيدًا عن مجلد المشروع.
> **التاريخ:** 2026-08-30
> **البيانات:** 96 عملة × 30 يومًا (data30) — محاكاة **طبق الأصل** من منطق المشروع (2274 + 2157 + 2171 + 2273).
> **⚠️ ملاحظة حيادية:** هذه الوثيقة **خارج** مجلد المشروع (`QUANT_NQ`) وتُحفظ بالمكان الذي تحدده. **لم يُعدَّل أي ملف داخل المشروع**، ولم تُغيَّر أي عتبة فيه. كل ما هنا وصفٌ وتحليل، لا تعديل على الشيفرة المشغَّلة.

---

## ١) ما هو فلتر المرساة؟

**مشكلة:** في ذرة 2274 (مُصنِّف الدخول)، تُؤخذ **"المرساة"** (سعر دخول - entry) من **مستوى فني قياسي** وليس من سعر السوق اللحظي:
- الصنف **①** (عودة لحافة القيمة): المرساة = `val` (لونغ) أو `vah` (شورت) — **حافة منطقة القيمة.**
- الصنفان **② و③** (كسور): المرساة = **`pdh` / `pdl` (مستوى يوم الأمس)** — يُستخرج من `prior_day_levels`.

**النتيجة:** المرساة قد تكون **بعيدة عن سعر اللحظة** الذي عندها نُطلقت الإشارة. مثال واقعي من ENA:
- close وقت الإشارة = **0.13954** ، المرساة = **0.14110** ⇒ فرق **+112 bps** (أكثر من 1%).
- وهو بالضبط ما لاحظه صاحب المشروع: "شورت بمرساة بعيدة عن السعر".

> أي أن "إشارة تُطلَق بينما سعر الدخول أبعد من السعر الحالي" = **تجربَ دخول بسيطاب/ارتكاس لم يُحدَّث للّحظة** — لذا كانت **كاذبة** (لا تُنفّذ فعلًا) أو **خاسرة** (دخول عند مستوى بعيد ثم سعر يرتد).

**فلتر المرساة:** شرط يرفض الإشارة قبل دخولها، إذا كان **بُعد المرساة عن الإغلاق الحالي** يتجاوز حدًّا معياريًّا:

```
gap_bps = (anchor − close) / close × 10_000
يُرفَض إذا: |gap_bps| > MAX_ANCHOR_BPS
```

---

## ٢) صيغة الفلتر (بالتفصيل)

```
anchor   = مستوى الدخول القادم من الذرة (val/vah أو pdh/pdl — حسب الصنف)
close    = سعر الإغلاق للشمعة التي أُطلقت عندها الإشارة
gap_bps  = (anchor − close) / close × 10_000     # بُعد المرساة عن السعر (نقاط أساس)

ACCEDE (cross/gate):
  إذا  |gap_bps|  ≤  MAX_ANCHOR_BPS   → الإشارة صالحة وتُمرَّر.
  إذا  |gap_bps|  >  MAX_ANCHOR_BPS   → الإشارة تُرفَض (لا تُنفَّذ) + يُعَدّ رفضًا "مرساة بعيدة".
```

**موضع التطبيق في سلسلة التقييم:** بعد أقفال الذرة (licence/transition/fade/رفض/وقود/كسر) **وقبل** احتساب `filled` (التحقق من تعبئة الدخول). أي أنه بوّابة على المرشّح، لا على الهدف/الخروج.

**مصدر `close`:** سعر إغلاق شمعة الإشارة نفسها (`c[i]`) — وليس سعر المرساة وليس سعر دخول مستقبلي (بلا تسريب معلومات مستقبلية).

---

## ٣) العتبات

### القيم المختبَرة (مسح شبكة 192 تكوينًا × مجموعات)
مرساة من **{بدون، 10، 20، 30، 40، 50، 60، 80، 100، 120، 150، 200}** bps.

### القيمة المحفوظة حاليًّا في الأدوات
| موضع | القيمة |
|---|---|
| `faithful_test96.py` / `faithful4_test.py` | `MAX_ANCHOR_BPS = 40.0` |

### القيمة الموصى بها (الأفضل كما كشف المسح)
**`MAX_ANCHOR_BPS ≈ 120 bps`** (نطاق إيجابي) توازنًا بين كبح الكاذب/الخاسر وعدم قطع الرابحات.

| المعيار | مشروعك الحالي (محكمة، بلا فلتر) | + مرساة **40** | + مرساة **120** ⭐ |
|---|---|---|---|
| **sumR (الكل 96)** | 381.85 | ~حواليه | **388.05** ⬆ |
| خسارة | 332 | 259 (على منطق الأعرض) | **316** ⬇ |
| كاذبة | 740 | 249 | **572** ⬇ |
| خسارة% / ربح% | 33.1 / 67.0 | 29.9 / 70.1 | **32.0 / 68.0** |

> **استنتاج مهم:** كلما ضاقت عتبة المرساة (مثل 40) قلّت الكاذبة والخسارة **لكن قلّ الربح** (تُقتطع رابحات). وعتبة **120** تترك لك **أعلى sumR** مع **أقل خسارة وكاذب** من غير فلتر. **فلا تستخدم عتبة ضيقة جدًّا إذا كان الهدف صافي الربح** — استخدم ~120.

---

## ٤) أين يوضع هذا الفلتر؟

1. **في أدوات الاختبار الحالية (داخل `crypto_calibration`):**
   - `faithful_test96.py` (سطر `MAX_ANCHOR_BPS = 40.0`)
   - `faithful4_test.py` (سطر `MAX_ANCHOR_BPS = 40.0`)
   - `faithful_sweep.py` (يتفحص شبكة القيم، وليس عتبة ثابتة)
   - الفحص يُطبَّق على كل مرشّح عبر: `if abs(gap) > MAX_ANCHOR_BPS: continue` (تحسب `gap` من `close=pr["c"][i]`، `anchor` = مرساة المرشّح).

2. **في المشروع نفسه (`QUANT_NQ` / ذرة 2274):** **لم يُطبَّق** — احترامًا لتعليم الرقابة ("لا تقترب من ذرات"، "لا تعديل في QUANT_NQ"). **الفلتر الآن وصف/تحليل، وإذا رغبتَ بدمجه في المشروع فلابد من موافقتك الصريحة** وسيُوضع داخل `_classify` (أو بعد إرجاع مرشّح) كبوّابة قبل العودة، لا داخل `_evaluate` الحُكميّ.

> **ملاحظة:** أُمكّن الإضافات فقط عند طلبك؛ الوثيقة حاليًّا **لا تغيّر** أي سلوك في المشروع.

---

## ٥) لماذا هذه العتبة بالذات؟ (مبرِّر)

- **بُعد المرساة عن السعر** = مقياس "عدم التطابق بين مستوى الدخول وسعر السوق". عند **≤120 bps (1.2%)** يعتبر الدخول منطقيًّا (السعر في محيط المستوى). عند أكبر، الإشارة غالبًا "متأخرة" أو "كاذبة" (السعر ابتعد عن المستوى ولم يرتدّ إليه).
- **التوافق مع الصنف ② (المصدر الغالبي):** مرساة صنف ② = **مستوى يوم الأمس**. لذا فبُعدها عن close يتغيّر طبيعيًّا خلال اليوم؛ عتبة **~1.2%** في نطاق السوق تقبض أكثر ما يهمّ بعيدًا دون قتل الصفقات القريبة.

---

## ٦) ملاحظة الصدق (ملزمة)

1. **الفلتر نُمذجَ في المحاكاة — لم يُدمَج في المشروع.** أرقامه تُظهر أثر المحاكاة على بيانات `data30`.
2. **انحياز البيانات:** الحيولة على **صنف ② (كسر مستوى الأمس)** أساسًا؛ الصنف ① شبه معطّل (إشارة قليلة). **مصدر "المرساة البعيدة" صنف ② غالبًا.**
3. **OI متاح لـ17 عملة فقط** ⇒ **محكمة OU/صنف③ تعمل عليها فقط**، والـ79 بلا OI تعتمد ①/②.
4. **المحكمة نُمذجت على VETO فقط** (aggressor+OI)؛ حسّات walls/premium/lens بيانات حية غير متوفرة تاريخيًّا.
5. **لا تغيير في عتبات المشروع** — بقيت كما هي. العتبات هنا **مقترَحة** لفلتر جديد يُضاف عند الطلب.
